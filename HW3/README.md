# Homework 3
By running the following cammand, the model will be trained. And the final result will be saved as `bert-base-uncased-finetuned`

```
accelerate launch --config_file default_config.yaml main.py
```
